# Devnet: signers for program deployment

signer "resolve_authority" "svm::secret_key" {
    description = "Authority that can call resolve (must match RESOLVE_AUTHORITY in program)"
    keypair_json = "./resolve_authority.json"
}
